{
  "name": "isai-illam-backend",
  "version": "1.0.0",
  "description": "Backend for Isai Ellam event registration",
  "main": "server.js",
"scripts": {
  "build": "cd .. && npm install && npm run build && cd backend && npm install",
  "start": "node server.js",
  "dev": "nodemon server.js"
}   
,
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "helmet": "^6.0.1",
    "morgan": "^1.10.0",
    "nodemailer": "^6.9.1",
    "pdf-lib": "^1.17.1",
    "qr-image": "^3.2.0",
    "uuid": "^9.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}