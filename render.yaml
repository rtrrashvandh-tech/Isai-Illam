services:
  - type: web
    name: isai-illam
    env: node
    buildCommand: chmod +x render-build.sh && ./render-build.sh
    startCommand: node backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000